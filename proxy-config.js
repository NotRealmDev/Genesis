/*
 Genesis Proxy configuration.

 After you deploy genesis-proxy-worker.js to Cloudflare Workers,
 paste the Worker URL below, for example:

 endpoint: "https://genesis-proxy.YOURNAME.workers.dev"

 Leave NO trailing slash.
*/
window.GENESIS_PROXY = {
  endpoint: "PASTE_YOUR_WORKER_URL_HERE"
};
